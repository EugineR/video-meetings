# apps/web — architecture reference

The per-file inventory of the Next.js frontend. It used to live in `apps/web/CLAUDE.md`,
where every session loaded all 13 KB of it before doing anything. Read the part you need;
`apps/web/CLAUDE.md` keeps the rules and the boundaries.

> Related: [testing](../testing/web.md) · [apps/api architecture](./api.md)

## Pages, components and libraries

Next.js App Router app, wired up with Tailwind CSS v4 and HeroUI v3. No longer the default `create-next-app` scaffold content — `page.tsx`/`layout.tsx` were replaced with a minimal client-side-authenticated app shell:

- `src/app/layout.tsx` — root layout; `body` carries HeroUI's semantic `bg-background text-foreground` classes; page `metadata` (title/description) set for this app rather than the `create-next-app` defaults
- `src/app/(app)/layout.tsx` — layout of the authenticated route group (`/`, `/meetings/[id]`, `/profile`, `/profile/edit`). Renders `AuthenticatedUserProvider` (the one auth guard and the one place `useAuthenticatedUser()` is called — see `components/layout/` below) wrapped around `AppShell` (background gradient, `AppHeader`, the centered `max-w-2xl` content container). Every page in the group therefore renders content only: no shell, no header, no `!user` spinner of its own, and the header is identical on all four routes — including `/meetings/{id}`, which used to pass only `email` to its own `AppHeader` and so lost the avatar.
- `src/app/(app)/error.tsx` — error boundary for that group: an unhandled render error inside a page renders a "Something went wrong" `Card` with the error message and a "Try again" `Button` calling Next's `reset()`, inside the shell (the header stays usable), instead of the dev overlay or a blank production page. It also `console.error`s the error once, in an effect.
- `src/app/(auth)/layout.tsx` — layout of the unauthenticated group (`/login`, `/register`): `AuthShell` — the same background, the brand-only `BrandHeader` (no user slot, no sign-out), and the centered `max-w-md` slot each page renders its `Card` into.
- `src/app/(app)/page.tsx` — home page (`/`), a client component (`'use client'`). Auth and the header come from the group layout above. An effect fetches `getMeetings()` (`src/lib/api.ts`, `GET /meetings`, returning `MeetingListItem[]` — `Meeting` plus `recordingCount`) and the page renders a "+ Create meeting" `Button` (with `PlusIcon`) that opens `CreateMeetingModal`, then: a "Recent meetings" `Card` listing `MeetingRow`s for the 3 most recently dated meetings (client-side sort/slice — the API returns the full unsorted list, no `limit`/`sort` params exist), shown only when there is at least one meeting, and an "All meetings" `Card` listing every meeting as a `MeetingRow` with a `Total: N` count. An inline error (from `ApiError`) renders if the meetings fetch fails. Both lists pass `onUploaded` (a callback that increments a meeting's `recordingCount` in local state by id) down to `MeetingRow`, so a meeting appearing in both lists stays in sync after an upload from either one, without a refetch. `handleMeetingCreated` prepends the newly created meeting (`recordingCount: 0`) to local state and is passed to `CreateMeetingModal` as `onCreated` — a new meeting surfaces in "Recent meetings" too, but only when its date is actually the most recent, since that list is client-sorted by date.
- `src/app/(app)/meetings/[id]/page.tsx` — meeting detail page (`/meetings/{id}`), a client component reading the id via `useParams()`. Auth, shell and header come from `(app)/layout.tsx`; an effect fetches `getMeeting(id)` (`GET /meetings/:id`, returning `MeetingDetail` — `Meeting` plus `recordings: Recording[]`) and renders a `Button` (`variant="secondary"`, `ArrowLeftIcon`) that calls `router.back()`, the meeting's title/date/participants, and a "Recordings" `Card` starting with an always-present `RecordingUploader` (height-capped to 256px so it never grows past the 200–300px range regardless of state), pinned above every `meeting.recordings` entry rendered as a compact `RecordingCard` tile — there is no toggle between a single card and the uploader, so a user can keep adding files. `handleUploaded` appends the new `Recording` to `meeting.recordings`; `handleDeleted(recordingId)` filters it out; both update local state directly (no refetch) and bump `pollGenerationRef`. A second effect polls `getMeeting(id)` every 4 seconds whenever `meeting.recordings.some(r => r.status === 'UPLOADED' || r.status === 'PROCESSING')` **or** `isSummaryPending` (cleared once neither holds, or on unmount), so the page catches up with the API's background per-file transcription and summarization without a manual reload; the `pollGenerationRef` counter invalidates a poll response that resolves after a more recent local update, and a poll failure stops the interval and surfaces the same inline error as the initial load. `isSummaryPending` — whether `meeting.summary` has caught up with the current set of recordings — is derived via a fingerprint (`reconciledRecordingsSignature`, set during render rather than in an effect) of every recording's `id:status`: it only advances past the current fingerprint once every recording is terminal (`READY`/`FAILED`) _and_ the summary itself has settled, where "settled" means `summary.status === 'FAILED'`, or `summary === null` with no `READY` recording, or — the case that needs its own check rather than trusting `status === 'READY'` alone — `summary.status === 'READY'` **and** `summary.foldedRecordingIds` (order-independently, via the module-level `sameRecordingIds` helper) matches every currently-`READY` recording's id. That last condition exists because `apps/api`'s `update_meeting` agent tool can briefly settle `MeetingSummary.status` to `READY` mid-fold without updating `foldedRecordingIds` (see `docs/architecture/api.md`); without the `foldedRecordingIds` check, that transient snapshot reads as "caught up" and polling stops before the fold's real, final write (with the complete `actionItems`) lands — the bug this check exists to close. `isSummaryPending` is passed to `MeetingSummarySection` as `isUpdating`. A 404 `ApiError` (unknown or another user's meeting) renders as an inline "Meeting not found" message rather than a blank page.
- `src/app/(auth)/login/page.tsx` — `/login`, a client component mirroring `register/page.tsx`'s `Card`/`Form` structure (email + password, show/hide toggle) but posting to `POST /auth/login` via `loginUser` (`src/lib/api.ts`). On success it stores the returned `accessToken` and redirects to `/`; on failure (e.g. wrong credentials → 401, same message regardless of which field was wrong) it renders the API's error message inline. Footer links to `/register` for new users.
- `src/app/(auth)/register/page.tsx` — `/register`, a client component rendering a HeroUI `Card`/`Form` that posts an email/password to `POST /auth/register` via `registerUser` from `src/lib/api.ts`. On success it stores the returned `accessToken` (`storeAccessToken`, `src/lib/auth.ts`) and redirects to `/`; on failure (e.g. duplicate email → 409) it renders the API's error message inline. Client-side validation distinguishes "email is required" from "invalid email format", enforces an 8-character password minimum, and the password field has a show/hide toggle; controls are sized to the 44px (mobile) / 40px (desktop) touch-target minimum. Footer links to `/login` for existing users.
- `src/components/` — one component per file, grouped into subfolders by kind rather than kept flat:
  - `components/layout/PageShell.tsx` — the full-height gradient background wrapper (`bg-linear-to-br from-accent/10 via-background to-background`). The one file that carries that class string; both shells below wrap their content in it.
  - `components/layout/BrandHeader.tsx` — the header bar itself: the app logo/name (as the page's `h1`) wrapped in a HeroUI `Link` to `/` on the left, and a right-hand `children` slot. Rendered empty-slotted by `AuthShell` (sign-in and registration have no user to show) and slot-filled by `AppHeader`.
  - `components/layout/AppHeader.tsx` — the authenticated header: `BrandHeader` plus, in its slot, a `UserAvatar` and the display name (falling back to `email`) wrapped in a HeroUI `Link` to `/profile`, and a "Sign out" `Button`. Purely presentational — `email`/`onSignOut` are required props, since it only renders where a session exists — and rendered in exactly one place, `AppShell`.
  - `components/layout/AppShell.tsx` — `PageShell` + `AppHeader` (fed from `useAuthenticatedUserContext()`) + the centered `max-w-2xl` content container, used by `(app)/layout.tsx`.
  - `components/layout/AuthShell.tsx` — `PageShell` + `BrandHeader` + the centered `max-w-md` card slot, used by `(auth)/layout.tsx`.
  - `components/layout/AuthenticatedUserProvider.tsx` — calls `useAuthenticatedUser()` once for the whole authenticated group, renders the single `if (!user) return <Spinner aria-label="Loading"/>` guard screen until the client-side check resolves, and puts the result (with `user` narrowed to non-null) on a context read via the exported `useAuthenticatedUserContext()`. Sharing one hook instance is what lets `/profile/edit` update the header through `applyProfile`/`applyAccessToken`, and keeps the profile fetched once per mount of the group rather than once per page.
  - `components/meetings/CreateMeetingModal.tsx` — a HeroUI `Modal.Backdrop`/`Form` (controlled via `isOpen`/`onOpenChange`) for creating a meeting by hand: `title` (required, inline error if blank), `date` (native `<input type="datetime-local">`, required, converted via `new Date(value).toISOString()` on submit), `participants` (a plain text `Input`, split on `,` and trimmed into a string array, `[]` — never omitted — when blank, since `CreateMeetingDto.participants` has no `@IsOptional()`). Submitting with a missing title/date sets inline errors and never calls the API. On success it calls `createMeeting()` (`src/lib/api.ts`), then the caller-supplied `onCreated(meeting)` and closes itself; on failure it shows the `ApiError` message inline and stays open. Not scoped to any existing meeting — used only by `page.tsx`.
  - `components/meetings/MeetingRow.tsx` — a single meeting's card row (title, calendar-icon-prefixed formatted date, users-icon-prefixed comma-joined participant list), used by `page.tsx` for both the "Recent" and "All meetings" lists; takes an optional `highlighted` prop for the blue-tinted "Recent meetings" styling. The title/date/participants sit inside a plain `<button>` (not a HeroUI `Button`, so it composes cleanly with block content) whose `onClick` does `router.push('/meetings/{id}')`; below it, a separate row shows either a `Chip` reading "N file"/"N files" (when `meeting.recordingCount > 0`) or an "Upload" `Button` that opens `UploadRecordingModal` for the zero-file case — kept as a sibling rather than nested inside the navigation button so clicking it can't also trigger navigation, with no `stopPropagation` needed.
  - `components/meetings/UploadRecordingModal.tsx` — a HeroUI `Modal.Backdrop` (controlled via `isOpen`/`onOpenChange` props, no `<Modal>` trigger wrapper) around `RecordingUploader`, used by `MeetingRow` for uploading without leaving the list.
  - `components/meetings/RecordingUploader.tsx` — the upload area: a dashed drop zone with a hidden `<input type="file">` triggered by a "Choose file" button, plus native drag-and-drop handlers. Validates MIME type and size client-side (`ALLOWED_MIME_TYPES`/`MAX_SIZE_BYTES` constants — mirror, and must be kept in sync with, `apps/api/.env`'s `ALLOWED_RECORDING_MIME_TYPES`/`MAX_UPLOAD_SIZE_BYTES`) before calling `uploadMeetingRecording`; shows a HeroUI `ProgressBar` during upload with a "Cancel" button that aborts via `AbortController`, and an inline error message (from `ApiError`, or the client-side validation message) otherwise. The drop zone, file input and drag-over handling all no-op while `isUploading` is true, so a second file dropped mid-upload can't hijack `abortControllerRef` out from under the first one's in-flight request. Used by both `/meetings/[id]/page.tsx` (rendered unconditionally below the recordings list, so a meeting with zero or several files can always accept another) and `UploadRecordingModal` (inside a modal).
  - `components/meetings/RecordingCard.tsx` — one recording's compact tile, keyed by `recording.id`, with no always-visible player: a play icon, the filename as a HeroUI `Link` (turns accent/blue with a pointer cursor on hover, `onPress` opens `RecordingPlayerModal`), human-readable size and upload date, a `RecordingStatusChip`, and a red icon-only ghost "Delete" `Button` (`text-danger`, no filled background) anchored to the row's right edge (opens a HeroUI `Modal.Backdrop` confirmation, not `window.confirm`, then calls `deleteMeetingRecording(meetingId, recording.id)` and `onDeleted(recording.id)` so the parent removes just that one entry — there is no "Replace" action, a user who wants to replace a file deletes it and uploads a new one via the always-present `RecordingUploader`). Below that row sits a single slot shared by three mutually exclusive states, kept at the "Show transcript" button's own min-height (`min-h-[44px] md:min-h-10`) across all three specifically so a recording finishing transcription (or transcription starting) never shifts the rest of the tile — a `FAILED` recording shows the existing failure notice; `status === 'UPLOADED'`/`'PROCESSING'` shows a `Spinner` + "Transcribing…"; `status === 'READY'` with `recording.transcriptText` present shows a blue-accent "Show transcript"/"Hide transcript" toggle button (`ChevronDownIcon` that rotates 180° when open, `aria-expanded`) that expands/collapses an inline panel with the transcript text (scrollable, whitespace preserved) rendered inside the tile itself, collapsed by default via local `isTranscriptOpen` state, never shown inside the player modal. The panel itself is always mounted (not conditionally rendered) inside a `grid-rows-[0fr]`/`grid-rows-[1fr]` wrapper (`transition-[grid-template-rows]`, `motion-reduce:transition-none`) with an `overflow-hidden` inner layer — the well-known CSS-only "animate to auto height" technique — so opening/closing grows/shrinks it smoothly instead of an instant show/hide; `aria-hidden` tracks `isTranscriptOpen` on that wrapper so assistive tech skips the clipped-to-zero-height content while collapsed. A recording with no ready transcript (`READY` with an empty `transcriptText`) shows nothing in the slot.
  - `components/meetings/RecordingPlayerModal.tsx` — a HeroUI `Modal.Backdrop`/`Modal.Container` (controlled via `isOpen`/`onOpenChange` props from `RecordingCard`) holding only the recording's `<audio controls>` (for `audio/mpeg`) or `<video controls>` (other MIME types) player, `src` from `getRecordingContentUrl(meetingId, recording.id)` — the same mimeType-keyed element choice `RecordingCard` used inline before the redesign. Playback only; it never renders transcript content.
  - `components/meetings/RecordingStatusChip.tsx` — maps a `RecordingStatus` (`UPLOADED`/`PROCESSING`/`READY`/`FAILED`) to a colored HeroUI `Chip` (`accent`/`warning`/`success`/`danger`); for the two in-progress statuses (`UPLOADED`/`PROCESSING`) it also renders a small `color="current"` `Spinner` before the label, inheriting the chip's color, so an in-progress recording reads as actively working rather than a static label — `READY`/`FAILED` render the label alone. Used by `RecordingCard`.
  - `components/meetings/MeetingSummarySection.tsx` — renders `meeting.summary`: a failure notice when `status === 'FAILED'`; a full `Spinner` "Generating summary…" while `status` is `PENDING`/`PROCESSING` or `summary` is still `null`; otherwise (`READY`) the summary text, an "Action items" list (each item's `description` plus an optional accent `Chip` for its `assignee`, or "No action items identified." when empty), and a "Decisions" bullet list (or "No decisions recorded."). Takes an `isUpdating` prop (`MeetingDetailPage`'s `isSummaryPending`) that, when true on an otherwise-`READY` summary, renders a small inline `Spinner` + "Updating — more recordings are still being processed…" banner above the content instead of hiding it behind the full processing spinner — content and banner coexist because there is real (if possibly not-yet-final) data to show. Used only by `meetings/[id]/page.tsx`, inside a "Summary" `Card` shown by `showSummarySection` whenever a summary row exists or a non-`FAILED` recording is still on its way to producing one.
  - `components/icons/` — one small inline SVG icon component per file (`EyeIcon.tsx`/`EyeOffIcon.tsx` for password visibility toggles, `VideoCameraIcon.tsx` for the app logo and the recording-count badge, `CalendarIcon.tsx`/`UsersIcon.tsx` for meeting metadata, `UploadIcon.tsx`/`TrashIcon.tsx`/`XMarkIcon.tsx` for recording actions, `PlayCircleIcon.tsx` for a recording tile's play indicator, `ChevronDownIcon.tsx` for the inline transcript expand/collapse toggle, `ArrowLeftIcon.tsx` for the meeting detail page's back button, `PlusIcon.tsx` for the home page's "Create meeting" button), re-exported through `icons/index.ts` so call sites import from `@/components/icons` rather than each file individually.
- `src/lib/api.ts` — thin `fetch` wrapper for calling `apps/api`: reads the base URL from `NEXT_PUBLIC_API_URL` (env var, see below), exposes `registerUser`, `loginUser`, `createMeeting(title, date, participants)`, `getMeetings`, `getMeeting`, `deleteMeetingRecording(meetingId, recordingId)` and `getRecordingContentUrl(meetingId, recordingId)` (all attaching `Authorization: Bearer <token>` via `getAccessToken()` from `src/lib/auth.ts`, except `getRecordingContentUrl` which appends the token as a `?token=` query param instead, since a `<video>` element's `src` can't set request headers and the API's `JwtAuthGuard` accepts either form), and throws `ApiError` (with the HTTP status and the API's `message`) on non-2xx responses. The shared `handleResponse` returns `undefined` for a 204 (no body to parse) rather than always calling `response.json()`, so `deleteMeetingRecording` can reuse it like every other function here instead of hand-rolling its own error handling. `uploadMeetingRecording(meetingId, file, opts)` is the one exception to the shared `fetch`-based `handleResponse` plumbing: it's built on `XMLHttpRequest` because `fetch` exposes no upload-progress events, posting to `/meetings/:id/recordings` and taking an `{ onProgress, signal }` options object (an `AbortSignal` cancels the in-flight `XMLHttpRequest`, rejecting with `UploadCancelledError` rather than `ApiError` so callers can distinguish a user-initiated cancel from a real failure) and reusing the same error-message extraction as the rest of the file.
- `src/lib/auth.ts` — client-side-only auth state, backed by `localStorage` (key `accessToken`, no cookie/session — nothing server-side reads it yet, so there's no SSR/middleware auth gate, only the client-side check via `useAuthenticatedUser`). `storeAccessToken`/`clearAccessToken` write/clear the token, `getAccessToken()` reads the raw token (used by `src/lib/api.ts` to authenticate requests); `getStoredUser()` decodes the JWT's payload (base64, no signature verification — the API is the source of truth) and returns `{ email }`, or `null` if missing/expired (`exp` claim), self-clearing the stored token when expired.
- `src/lib/useAuthenticatedUser.ts` — `useAuthenticatedUser()`, the client-side session hook: redirects to `/login` if `getStoredUser()` finds nothing valid, otherwise exposes `{ user, profile, profileError, signOut, applyAccessToken, applyProfile }`; `user` stays `null` until the check resolves. That check is keyed to `usePathname()` rather than to mount, because the provider that owns the hook sits in a layout Next.js keeps mounted across in-group navigation — without it an expired token would go unnoticed until a full page load; it reuses the previous `user` object when the email is unchanged, so re-validating never refetches the profile. It is called in exactly one place — `AuthenticatedUserProvider` — which turns "`user` is still null" into the group's one guard screen and hands the rest to pages through context; a page calling this hook itself would issue a second `GET /users/me` and hold a profile copy the header never sees.
- `src/lib/format.ts` — `formatDateTime()`, the single `Intl.DateTimeFormat('en-US', { dateStyle: 'medium', timeStyle: 'short' })` used everywhere a meeting or recording timestamp is displayed (`MeetingRow`, `RecordingCard`, `meetings/[id]/page.tsx`).
- `src/app/globals.css` — global styles. Starts with `@import 'tailwindcss';` then `@import '@heroui/styles';` (order matters); the old hand-rolled `--background`/`--foreground` light/dark vars were removed in favor of HeroUI's own theme variables (see below)
- `postcss.config.mjs` — registers the `@tailwindcss/postcss` plugin, required for Tailwind v4

## Calling apps/api

`NEXT_PUBLIC_API_URL` (see `.env.example`, defaults to `http://localhost:3001` in `src/lib/api.ts` if unset) is the base URL `src/lib/api.ts` calls. It must be prefixed `NEXT_PUBLIC_` to be readable in client components (Next.js only inlines env vars with that prefix into the browser bundle). It must point at `apps/api`'s port (`PORT` in `apps/api/.env`, default `3001`).

## HeroUI v3

- Packages: `@heroui/react`, `@heroui/styles`, `tailwind-variants`, plus `tailwindcss`/`@tailwindcss/postcss`/`postcss` for the Tailwind v4 base it builds on.
- No provider component is needed (unlike HeroUI v2's `HeroUIProvider`).
- Components use compound composition (e.g. `Card.Header`) and `onPress` instead of `onClick`; import from `@heroui/react`.
- Theming is CSS-variable/`oklch`-based via `@heroui/styles`, switching on `[data-theme='dark']`/`[data-theme='light']` rather than `prefers-color-scheme`. No theme switcher is wired up yet — the app currently renders HeroUI's default light theme (`:root`'s values apply with no `data-theme` attribute set), except `src/app/globals.css` darkens HeroUI's default `--accent`/`--muted` tokens to meet WCAG AA contrast (4.5:1) — HeroUI's originals fell short for button text on `--accent` and placeholder text on `--muted`.

## Lint

ESLint config (`eslint.config.mjs`) composes `eslint-config-next`'s `core-web-vitals` and `typescript` rule sets via the flat-config `defineConfig` helper.
